aiofiles==0.8.0          # Updated to a later version
aiohttp==3.8.1           # Updated to a later version for Python 3.8 compatibility
fastai==2.7.10           # Updated to a compatible version with Python 3.8
torch==1.13.1            # Updated to a later version (ensure compatibility with your needs)
torchvision==0.14.1      # Updated to a later version (ensure compatibility with your needs)
numpy==1.23.5            # Updated to a later version for compatibility
starlette==0.20.4        # Updated to a later version
uvicorn==0.22.0          # Updated to a later version
python-multipart==0.0.6  # Updated to a later version
